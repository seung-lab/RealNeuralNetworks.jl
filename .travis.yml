language: julia
os: linux

julia: 
    - 0.6
    - nightly

notifications: 
    email: false

git: 
    depth: 99999

script:
    - julia -e 'Pkg.add("HDF5")'
    - julia -e 'Pkg.clone(pwd()); Pkg.test("RealNeuralNetworks")'
